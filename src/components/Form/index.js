
import React from 'react';
import PropsTypes from 'prop-types';

const Form = () => (
  <div id="form">
    <form>
      <input type="text" />
    </form>
  </div>
);

Form.propTypes = {
};

export default Form;
